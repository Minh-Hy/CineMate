<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bo nhẹ góc nếu thích (không bắt buộc) -->
    <corners android:radius="0dp"/>

    <!-- Linear gradient: trên đen -> dưới đỏ đậm -->
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="#041B2D"
        android:endColor="#004E9A" />   <!-- đỏ đậm phía dưới (#E50914 nếu muốn đỏ Netflix) -->
</shape>
